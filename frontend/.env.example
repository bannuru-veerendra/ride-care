# Dev: leave as /api so Vite proxies to 127.0.0.1:8000 (same-origin cookies,
# no CORS preflight, no Windows localhost IPv6 delay).
VITE_API_URL=/api

# Optional: absolute URL to hit an API directly (slower on Windows).
# VITE_API_URL=http://127.0.0.1:8000
