# Local: point at the FastAPI server directly.
VITE_API_URL=http://localhost:8000

# Production (Vercel): use /api — rewritten to Render in vercel.json
# so auth cookies stay first-party on the Vercel domain.
# VITE_API_URL=/api
